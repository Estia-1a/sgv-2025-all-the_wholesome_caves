/* Suppression des erreurs W3C : Doctype et Langue sont gérés dans l'HTML */

body {
    font-family: 'Segoe UI', sans-serif;
    background-color: #f4f4f4;
    margin: 0;
    display: flex;
    justify-content: center;
}

.mobile-container {
    width: 375px; /* Largeur standard smartphone */
    background-color: white;
    min-height: 100vh;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
    position: relative;
    padding-bottom: 70px;
}

/* En-tête En ce moment */
.status-card {
    background-color: #fff;
    margin: 15px;
    padding: 15px;
    border: 2px solid #000;
    border-radius: 20px;
}

.status-header {
    display: flex;
    align-items: center;
    gap: 10px;
}

/* Accès rapide */
.quick-access {
    padding: 0 15px;
}

.card {
    border: 2px solid #000;
    border-radius: 15px;
    margin-bottom: 15px;
    overflow: hidden;
}

.card-header {
    padding: 10px;
    font-weight: bold;
    border-bottom: 1px solid #eee;
}

.calendar-img {
    width: 100%;
    display: block;
}

/* Liste d'actions */
.action-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.action-item {
    display: flex;
    align-items: center;
    gap: 15px;
    padding: 12px;
    border: 2px solid #000;
    border-radius: 25px;
    text-decoration: none;
    color: black;
    font-weight: 500;
}

/* Navigation Basse */
.bottom-nav {
    position: absolute;
    bottom: 0;
    width: 100%;
    height: 60px;
    display: flex;
    justify-content: space-around;
    align-items: center;
    border-top: 1px solid #ccc;
    background: white;
}

.bottom-nav img {
    width: 30px;
}
